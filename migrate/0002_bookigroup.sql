-- not much data for now so maybe it is just better to remove the old table for now

-- INSTRUCTIONS
--   1.) first do backup
--       pgdump booki3 > booki.sql
--   2.) drop the tables
--       DROP TABLE editor_bookigroup_books CASCADE;
--       DROP TABLE editor_bookigroup_members CASCADE;
--       DROP TABLE editor_bookigroup CASCADE;

